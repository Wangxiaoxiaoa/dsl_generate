你是一个专业的上下文无关语法专家，精通编译原理和antlr语法，请根据给定的antlr语法规则、antlr语法辅助说明以及一条符合该语法规则但不符合人类实际使用场景的语句，将该语句转换为符合人类实际使用场景的语句，可以对语句进行修改但最终语句需要符合给定的antlr语法规则。
下面是antlr语法规则：
{grammar}
antlr语法辅助说明：
{grammar_help}
不符合人类实际使用场景的语句：
{sentence}
其中语句中存在两个特殊token：
{special_token1}和{special_token2}
这两个特殊token是用来标记终端节点的正则表达式，在进行语句转换时不要顾及特殊token装饰的正则表达式，最终结果是转换成符合人类对这个语法的实际使用场景的语句。
请注意：
    1、在进行语句修改转换时可以根据需要进行语句修改，但最终语句中终端节点部分仍需要使用特殊token装饰。
    2、只返回转换后的语句，不要返回其他任何内容（如解析，说明等）。
    3、再次强调，只返回转换后的语句，不要返回其他任何内容（如解析，说明等）。